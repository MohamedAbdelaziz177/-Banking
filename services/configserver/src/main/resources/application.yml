spring:
  application:
    name: configserver

  profiles:
    active: native

  cloud:
    config:
      server:
        native:
          search-locations: "classpath:/, classpath:/config, file:./, file:./config"

server:
  port: 8888
